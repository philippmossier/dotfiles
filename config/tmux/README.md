# before running tmux

- make sure tmux 3.3a or up is installed
- run `git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm` first
- then run `<tmux-prefix>I` for installing the plugins
- [optional] update plugins with `<tmux-prefix>U`
- [optional] remove/uninstall plugins not on the plugin list with `<tmux-prefix>alt+u`
